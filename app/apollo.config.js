module.exports = {
  client: {
    service: {
      //an alternative is, local copy can be downloaded using localSchemaFile
      url: "http://localhost:4000/graphql",
      skipSSLValidation: true,
    }
  }
}